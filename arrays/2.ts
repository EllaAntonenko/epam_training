//2. Given an array of car manufacturers, convert the array to a string and back to an array

const carManf = ['Lexus', 'Toyota', 'Land Rover']
console.log(carManf.join())
console.log(Array.from(carManf))