// 1. Given an array consisting of movie names, iterate over the array with the output of the names of each movie to the console

const films = ['Avatar', 'Titanic', 'Frozen']
    films.forEach(function(elem){
        console.log(elem)
})